import { StyleSheet } from 'react-native'

export default styles = (window) => {
  return StyleSheet.create({
    imageContainer: {
      flex: 1,
      flexDirection: 'row',
      alignItems: 'center',
    },
    menuBarImage: {
      marginLeft: 20,
      width: 20,
      height: 20
    },
  })
}
